import 'package:flutter/material.dart';

class LineSpacer extends StatelessWidget {
  const LineSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 14,
    );
  }
}